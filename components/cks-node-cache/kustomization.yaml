# Pull it into an overlay with:
#   components:
#     - ../../../components/coreweave-node-cache
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
  - path: node-local-cache.yaml
    target:
      kind: Deployment
  - path: node-local-cache-lws.yaml
    target:
      kind: LeaderWorkerSet
