# Cluster-agnostic aggregated (single-pod prefill+decode) Inkling deployment,
# plus its smoke-test Service and PodMonitors.
#
# Do not apply this directly -- pick an overlay:
#   kubectl apply -k aggregated/overlays/coreweave/inkling-small -n robshaw-dev
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - inkling-small.yaml
